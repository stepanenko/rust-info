// === Semantic Versioning (SemVer)=====
//"mongoose": "^5.2.17"    ^ Caret: same as 5.x (major.minor.patch(bug fixes))
//"underscore": "~1.9.1"   ~ Tilde: same as 1.9.x
// if you use no ^ or ~ the 'npm i' will install exact same version


// To see the list of all installed packages and its versions use 'npm list'
// or to see only your App's dependencies use 'npm list --depth=0'

// To see a list of a specific module dependencies (e.g 'mongoose')
// use 'npm view mongoose dependencies'

// To see a list of a specific module versions (e.g 'mongoose')
// use 'npm view mongoose versions'

// To install a specific version of a module (e.g 'mongoose')
// use 'npm i mongoose@2.4.2'